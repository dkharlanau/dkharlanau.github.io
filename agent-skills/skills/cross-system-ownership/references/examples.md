# Examples

## Order to external warehouse
Sales owns business completeness, integration team owns transport, warehouse consumer owns contract handling, master-data owner owns identifier semantics, and operations owns alert response. The map must still define who closes an end-to-end failed order.

## SaaS export
Platform support cannot be the only owner if business reconciliation and retry decisions belong elsewhere.