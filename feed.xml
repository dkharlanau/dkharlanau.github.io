---
layout: null
permalink: /feed.xml
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
  {% assign publications = site.blog | where: 'verified', true | where: 'status', 'reviewed' %}
  {% assign entries = publications | concat: site.data.changelog.entries | sort: 'date' | reverse %}
  {% assign latest = entries | first %}
  <id>{{ '/' | absolute_url }}</id>
  <title>{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <updated>{{ latest.date | date_to_xmlschema }}</updated>
  <link href="{{ page.url | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" />
  <link href="{{ '/rss.xml' | absolute_url }}" rel="alternate" type="application/rss+xml" />
  <author><name>{{ site.author.name | xml_escape }}</name></author>
  {% for entry in entries limit: 30 %}
    {% if entry.collection == 'blog' %}
      {% unless entry.robots contains 'noindex' %}
        {% unless entry.sitemap == false %}
  <entry>
    <id>{{ entry.url | absolute_url }}</id>
    <title type="html">{{ entry.title | xml_escape }}</title>
    <link href="{{ entry.url | absolute_url }}" rel="alternate" type="text/html" />
    <published>{{ entry.date | date_to_xmlschema }}</published>
    <updated>{{ entry.last_modified_at | default: entry.updated | default: entry.date | date_to_xmlschema }}</updated>
    <summary type="html">{{ entry.description | default: entry.excerpt | strip_html | normalize_whitespace | xml_escape }}</summary>
  </entry>
        {% endunless %}
      {% endunless %}
    {% else %}
  <entry>
    <id>tag:dkharlanau.github.io,{{ entry.date | date: '%Y-%m-%d' }}:changelog/{{ entry.title | slugify }}</id>
    <title type="html">{{ entry.title | xml_escape }}</title>
    <link href="{{ '/changelog/' | absolute_url }}" rel="alternate" type="text/html" />
    <published>{{ entry.date | date_to_xmlschema }}</published>
    <updated>{{ entry.date | date_to_xmlschema }}</updated>
    <summary type="html">{{ entry.summary | xml_escape }}</summary>
  </entry>
    {% endif %}
  {% endfor %}
</feed>
