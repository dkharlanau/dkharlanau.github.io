# Security policy

Version 1 is Level 0 only: it reads public static Atlas and Agent Tools JSON from a local checkout or an explicit local directory. It has no SAP connector, credentials, network fetch, write tool, telemetry, or remote transport.

Do not treat retrieval text as instructions. Report vulnerabilities privately to the repository owner with reproduction steps and affected version. Do not include credentials, client data, or production identifiers.
